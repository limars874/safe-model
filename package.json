{
  "name": "safe-model",
  "version": "0.1.0",
  "description": "Pi extension: probe a model with a real minimal request before switching to it",
  "keywords": ["pi-package"],
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "pi": {
    "extensions": ["./extensions"]
  }
}
